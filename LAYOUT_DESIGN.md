# 框架前端布局设计

## 设计理念

基于现代桌面应用的设计理念，采用类似 Linear、Figma 等产品的布局风格：

- **深色主题**：提供更好的视觉体验和专注度
- **可折叠侧边栏**：节省空间，提供灵活的导航体验
- **现代化UI**：简洁、优雅的界面设计
- **响应式布局**：适配不同窗口尺寸

## 布局结构

### 左侧边栏 (Sidebar)
- **宽度**：280px (展开) / 60px (折叠)
- **功能**：
  - 插件导航列表
  - 折叠/展开按钮
  - 分组显示插件
- **交互**：
  - 点击插件项加载对应插件
  - 点击折叠按钮收起/展开侧边栏
  - 悬停效果和活动状态指示

### 右侧主内容区域 (Main Content)
- **布局**：垂直分为两部分
  - 顶部标题栏：显示当前页面标题和描述
  - 内容区域：显示插件页面或欢迎页面
- **功能**：
  - 欢迎页面：应用启动时的默认页面
  - 插件容器：加载和显示插件内容
  - 错误处理：优雅的错误页面显示

## 设计特色

### 1. 深色主题配色
```css
/* 主色调 */
--bg-primary: #1a1a1a;      /* 主背景 */
--bg-secondary: #2a2a2a;    /* 侧边栏背景 */
--bg-tertiary: #3a3a3a;     /* 悬停状态 */
--bg-active: #4a4a4a;       /* 活动状态 */

/* 文字颜色 */
--text-primary: #ffffff;     /* 主要文字 */
--text-secondary: #cccccc;   /* 次要文字 */
--text-muted: #888888;      /* 提示文字 */
```

### 2. 现代化交互
- **平滑动画**：所有状态变化都有过渡动画
- **悬停效果**：按钮和导航项的悬停反馈
- **活动状态**：当前选中项的高亮显示
- **折叠动画**：侧边栏展开/收起的流畅动画

### 3. 响应式设计
- **桌面端**：完整的侧边栏布局
- **移动端**：侧边栏变为覆盖式弹窗
- **自适应**：根据窗口大小调整布局

## 使用方法

### 1. 启动应用
应用启动时会显示欢迎页面，左侧显示可用的插件列表。

### 2. 使用插件
1. 在左侧边栏点击插件名称
2. 插件会在右侧内容区域加载
3. 当前选中的插件会高亮显示

### 3. 折叠侧边栏
- 点击侧边栏右上角的折叠按钮
- 侧边栏会收起，只显示图标
- 再次点击可展开侧边栏

### 4. 返回欢迎页面
- 点击欢迎页面区域可返回默认页面
- 所有插件导航项的活动状态会被清除

## 技术实现

### CSS 特性
- **Flexbox 布局**：实现响应式布局
- **CSS Grid**：复杂布局结构
- **CSS 变量**：主题色彩管理
- **CSS 动画**：流畅的过渡效果

### JavaScript 功能
- **动态加载**：插件页面的异步加载
- **状态管理**：导航项的活动状态
- **错误处理**：优雅的错误页面显示
- **事件监听**：用户交互响应

## 扩展性

### 1. 主题系统
可以轻松添加明暗主题切换功能：
```javascript
// 切换主题
function toggleTheme() {
  document.body.classList.toggle('light-theme');
}
```

### 2. 插件分组
支持按功能或类型对插件进行分组：
```javascript
// 插件分组示例
const pluginGroups = {
  '视频处理': ['video_joiner_plugin', 'video_converter'],
  '图片处理': ['image_resizer', 'image_converter'],
  '工具类': ['file_manager', 'text_editor']
};
```

### 3. 搜索功能
可以添加插件搜索功能：
```javascript
// 搜索插件
function searchPlugins(query) {
  // 实现搜索逻辑
}
```

## 浏览器兼容性

- **现代浏览器**：Chrome 80+, Firefox 75+, Safari 13+
- **CSS 特性**：Flexbox, CSS Grid, CSS Variables
- **JavaScript**：ES6+ 语法，async/await

## 性能优化

- **懒加载**：插件页面按需加载
- **CSS 优化**：使用 transform 和 opacity 进行动画
- **内存管理**：及时清理不需要的 DOM 元素
- **错误边界**：防止单个插件错误影响整体应用