# 插件化客户端系统架构文档

## 📋 目录

1. [系统概述](#系统概述)
2. [整体架构](#整体架构)
3. [客户端架构](#客户端架构)
4. [插件架构](#插件架构)
5. [通信机制](#通信机制)
6. [路径管理](#路径管理)
7. [开发工作流](#开发工作流)
8. [最佳实践](#最佳实践)

---

## 系统概述

这是一个基于 **Electron + Vue 3** 的插件化客户端系统，支持动态加载和运行多个独立插件。系统采用**主应用 + 插件**的分离架构，插件运行在独立的子进程中，通过 IPC 和 PostMessage 进行通信。

### 技术栈

#### 主应用
- **框架**: Electron 37.x + Vue 3 + TypeScript
- **UI库**: Element Plus
- **状态管理**: Pinia
- **路由**: Vue Router
- **构建工具**: Vite 5.x

#### 插件
- **后端**: Node.js (子进程)
- **前端**: Vue 3 / 原生 JavaScript + Vite
- **浏览器自动化**: Playwright
- **其他工具**: FFmpeg, Sharp, XLSX 等

---

## 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                         主应用 (Main)                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  窗口管理器   │  │  配置管理器   │  │  路径管理器   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  插件管理器   │  │  定时调度器   │  │  静态服务器   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              IPC Handlers (通用方法)                 │    │
│  └─────────────────────────────────────────────────────┘    │
└────────────────────────┬────────────────────────────────────┘
                         │ IPC 通信
┌────────────────────────┴────────────────────────────────────┐
│                    渲染进程 (Renderer)                        │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  Vue 3 应用 (主界面 + 路由)                          │    │
│  │  ┌──────────────┐  ┌──────────────┐                │    │
│  │  │  Home 页面    │  │ PluginView   │                │    │
│  │  └──────────────┘  └──────┬───────┘                │    │
│  │                            │ iframe 嵌入             │    │
│  │                    ┌───────┴────────┐              │    │
│  │                    │  插件前端界面    │              │    │
│  │                    └────────────────┘              │    │
│  └─────────────────────────────────────────────────────┘    │
└────────────────────────┬────────────────────────────────────┘
                         │ PostMessage
┌────────────────────────┴────────────────────────────────────┐
│              插件子进程 (Plugin Child Process)                │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  plugin_host.js (插件运行时宿主)                      │    │
│  │    ├─ 加载节点类 (nodes/*.js)                        │    │
│  │    ├─ 实例化并调用方法                                │    │
│  │    └─ 返回执行结果                                    │    │
│  └─────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  业务节点 (Nodes)                                     │    │
│  │    ├─ 登录节点 (LoginNode)                           │    │
│  │    ├─ 搜索节点 (SearchNode)                          │    │
│  │    └─ 处理节点 (ProcessNode)                         │    │
│  └─────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  工具类 (Utils)                                       │    │
│  │    ├─ 浏览器管理器 (browser-manager)                  │    │
│  │    ├─ Excel 读取器 (excel-reader)                    │    │
│  │    ├─ Cookie 管理器 (cookie-manager)                 │    │
│  │    └─ FFmpeg 定位器 (ffmpeg-locator)                 │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

---

## 客户端架构

### 1. 启动流程

```typescript
// electron/main.ts

1. app.whenReady() - 等待 Electron 应用就绪
2. staticServer.start() - 启动插件静态文件服务器（HTTP）
3. configManager.getConfig() - 读取 config.json 配置
4. tracker.login() - [可选] Tracker 登录上报
5. setupIpcHandlers() - 注册所有 IPC 通信处理器
6. windowManager.createMainWindow() - 创建主窗口
7. windowManager.loadContent() - 加载主界面 (dist/index.html)
```

**关键文件**:
- `electron/main.ts` - 应用入口
- `electron/window.ts` - 窗口管理
- `electron/config.ts` - 配置管理

### 2. 插件管理器 (PluginManager)

**核心职责**: 管理插件的生命周期和通信

#### 2.1 插件发现与加载

```typescript
// electron/services/plugin-manager.ts

getAvailablePlugins(): string[] {
  // 1. 扫描 plugins/ 目录
  // 2. 过滤出包含 manifest.json 和 plugin_host.js 的文件夹
  // 3. 读取 manifest.json 获取插件显示名称
  // 4. 建立显示名称 -> 文件夹名的映射
  // 5. 返回插件列表
}
```

#### 2.2 插件进程管理

```typescript
// 启动插件子进程
async startPluginProcess(pluginName: string) {
  // 1. 获取插件目录下的 plugin_host.js 路径
  // 2. 设置环境变量 (FFMPEG_PATH, CHROME_PATH)
  // 3. 使用 child_process.fork() 创建子进程
  // 4. 监听子进程消息 (message, exit, error)
  // 5. 缓存子进程实例到 Map<pluginName, childProcess>
}

// 触发插件事件
async triggerEvent(pluginName, eventType, params) {
  // 1. 读取 manifest.json 找到对应的事件配置
  // 2. 获取或创建插件子进程
  // 3. 发送消息到子进程: { jsFile, call: { class, method, args } }
  // 4. 等待子进程返回结果 (Promise + 超时保护)
  // 5. 返回执行结果
}
```

**插件进程缓存策略**:
- 首次调用插件事件时创建子进程
- 子进程保持运行，后续调用复用同一进程
- 子进程退出时自动从缓存中移除
- 应用退出时清理所有子进程

#### 2.3 插件停止机制

```typescript
stopPluginProcess(pluginName: string) {
  // 发送 { type: 'stop' } 消息到插件子进程
  // 插件可以实现优雅停止逻辑
}
```

### 3. IPC 通信处理器

**核心职责**: 为渲染进程提供主进程能力

```typescript
// electron/ipcHandlers.ts

主要 IPC 通道:
├─ select-file           - 选择文件对话框
├─ select-folder         - 选择文件夹对话框
├─ select-file-url       - 选择文件并转为 Data URL
├─ get-plugin-dirs       - 获取插件列表
├─ start-plugin-process  - 启动插件并获取 manifest
├─ trigger-event         - 触发插件事件
├─ get-plugin-http-url   - 获取插件前端 HTTP URL
├─ download-images-as-zip - 批量下载图片并打包为 ZIP
├─ open-folder           - 打开系统文件夹
├─ read-json-file        - 读取 JSON 文件
├─ start-scheduled-task  - 启动定时任务
├─ stop-scheduled-task   - 停止定时任务
└─ list-scheduled-tasks  - 列出所有定时任务
```

**白名单机制**:
- 所有可供插件前端调用的 IPC 通道需在 `config.json` 的 `ipcAllowlist` 中声明
- 插件前端通过 `invokeIpc(channel, args)` 调用
- 宿主会验证 channel 是否在白名单中

### 4. 定时调度器 (Scheduler)

**核心职责**: 管理插件的定时任务

```typescript
// electron/services/scheduler.ts

// 启动定时任务
startPluginScheduledTask(config: SchedulerConfig) {
  // 1. 使用 node-cron 创建定时任务
  // 2. 定时触发指定的插件事件
  // 3. 缓存任务配置和实例
}

// 配置结构
interface SchedulerConfig {
  pluginName: string      // 插件名称
  eventType: string       // 事件类型
  params: any            // 事件参数
  cronExpression: string // Cron 表达式，如 "0 */2 * * *"
  enabled: boolean       // 是否启用
}
```

### 5. 静态文件服务器

**核心职责**: 为插件前端提供 HTTP 访问能力

```typescript
// electron/static-server.ts

// 启动本地 HTTP 服务器
start() {
  // 1. 监听随机端口 (127.0.0.1:随机)
  // 2. 服务 plugins/ 目录下的文件
  // 3. 支持 CORS 和跨域资源加载
  // 4. 路径格式: http://127.0.0.1:端口/插件名/文件路径
}

// 访问示例
http://127.0.0.1:54321/taobao-scraper-plugin/dist/index.html
```

**为什么需要 HTTP 服务器？**
- iframe 加载本地文件 (`file://`) 存在跨域和安全限制
- HTTP 协议可以更好地支持模块化加载和资源访问
- 避免 CSP (Content Security Policy) 问题

### 6. 路径管理器 (PathManager)

**核心职责**: 统一处理开发环境和打包环境的路径差异

```typescript
// utils/path-manager.ts

主要方法:
├─ getAppRoot()          - 获取应用根目录
├─ getPluginsDir()       - 获取插件目录
├─ getPluginDir(name)    - 获取指定插件目录
├─ getPluginManifestPath(name) - 获取 manifest.json 路径
├─ getPluginHostPath(name)     - 获取 plugin_host.js 路径
├─ getFfmpegPath()       - 获取 FFmpeg 可执行文件路径
├─ getChromePath()       - 获取 Chrome 可执行文件路径
└─ getAvailablePlugins() - 扫描并返回所有可用插件
```

**路径适配逻辑**:
```typescript
// 开发环境
appRoot: /path/to/project
pluginsDir: /path/to/project/plugins

// 打包环境 (Windows)
appRoot: C:/Program Files/AppName
pluginsDir: C:/Program Files/AppName/resources/plugins
```

---

## 插件架构

### 1. 插件目录结构

```
插件名/
├── manifest.json           # 插件配置清单
├── plugin_host.js         # 插件运行时宿主（必需）
├── nodes/                 # 业务节点目录
│   ├── login.js          # 登录节点
│   ├── search.js         # 搜索节点
│   └── process.js        # 处理节点
├── utils/                 # 工具类目录
│   ├── browser-manager.js
│   ├── excel-reader.js
│   └── cookie-manager.js
├── frontend/              # 前端源码目录
│   ├── index.html
│   ├── main.js
│   ├── package.json
│   └── vite.config.ts
├── dist/                  # 前端构建产物
│   ├── index.html
│   └── assets/
├── node_modules/          # 插件依赖
└── package.json           # 插件 package.json
```

### 2. manifest.json 配置

```json
{
  "name": "插件显示名称",
  "version": "1.0.0",
  "description": "插件描述",
  "events": [
    {
      "id": "event-id",           // 事件唯一标识
      "class": "NodeClassName",   // 节点类名
      "method": "methodName",     // 方法名
      "jsFile": "nodes/xxx.js"    // 节点文件路径
    }
  ],
  "frontend": {
    "devServerUrl": "http://127.0.0.1:5175",  // 开发服务器地址
    "entry": "dist/index.html"                 // 生产环境入口
  }
}
```

**事件配置说明**:
- `id`: 前端调用时使用的事件标识
- `class`: 对应 jsFile 中导出的类名
- `method`: 类中的方法名
- `jsFile`: 相对于插件根目录的路径

### 3. plugin_host.js 运行时

**核心职责**: 接收主进程消息，加载节点类并执行方法

```javascript
// plugin_host.js

process.on('message', async (msg) => {
  // 1. 接收消息: { jsFile, call: { class, method, args } }
  
  // 2. 动态加载模块
  const mod = require('./' + msg.jsFile)
  
  // 3. 实例化类
  const Cls = mod[msg.call.class]
  const instance = new Cls()
  
  // 4. 调用方法
  const result = await instance[msg.call.method](...msg.call.args)
  
  // 5. 返回结果
  process.send({ success: true, result })
})
```

**停止信号处理**:
```javascript
let shouldStop = false

process.on('message', async (msg) => {
  if (msg?.type === 'stop') {
    shouldStop = true
    // 插件可以在长时间任务中检查 shouldStop 并优雅停止
  }
})
```

### 4. 节点类开发规范

```javascript
// nodes/example-node.js

class ExampleNode {
  constructor() {
    // 初始化资源（浏览器、数据库连接等）
    this.browser = null
  }

  /**
   * 业务方法 - 在 manifest.json 中配置
   * @param {string} param1 - 参数1
   * @param {number} param2 - 参数2
   * @returns {Promise<{success: boolean, data?: any, message?: string}>}
   */
  async businessMethod(param1, param2) {
    try {
      console.log('开始执行业务逻辑')
      
      // 执行业务逻辑
      const result = await this.doSomething(param1, param2)
      
      return {
        success: true,
        data: result,
        message: '执行成功'
      }
    } catch (error) {
      console.error('执行失败:', error)
      return {
        success: false,
        message: error.message || '执行失败'
      }
    }
  }

  async doSomething(param1, param2) {
    // 具体实现
    return { param1, param2 }
  }
}

// 导出类（注意：必须与 manifest.json 中的 class 名称一致）
exports.ExampleNode = ExampleNode
```

**最佳实践**:
1. 所有方法都应该是 `async`
2. 返回统一的结果结构: `{ success, data?, message? }`
3. 完善的错误处理和日志记录
4. 资源管理（浏览器、文件流等）需要及时释放

### 5. 插件前端开发

#### 5.1 前端通信封装

所有插件前端必须实现两个核心方法:

```javascript
// 前端通信封装 (ipc.js)

let seq = 0

// 触发插件后端事件
export const triggerEvent = async (eventType, params = {}) => {
  return new Promise((resolve, reject) => {
    const id = ++seq
    const timeout = setTimeout(() => reject(new Error('请求超时')), 600000)
    
    const messageHandler = (event) => {
      const data = event.data || {}
      if (data.source === 'host' && data.id === id) {
        window.removeEventListener('message', messageHandler)
        clearTimeout(timeout)
        if (data.success) resolve(data.result)
        else reject(new Error(data.error || '未知错误'))
      }
    }
    
    window.addEventListener('message', messageHandler)
    window.parent.postMessage({
      source: 'plugin-frontend',
      action: 'trigger-event',
      id,
      payload: { eventType, params }
    }, '*')
  })
}

// 调用主进程 IPC 方法
export const invokeIpc = async (channel, args = []) => {
  return new Promise((resolve, reject) => {
    const id = ++seq
    const timeout = setTimeout(() => reject(new Error('请求超时')), 600000)
    
    const messageHandler = (event) => {
      const data = event.data || {}
      if (data.source === 'host' && data.id === id) {
        window.removeEventListener('message', messageHandler)
        clearTimeout(timeout)
        if (data.success) resolve(data.result)
        else reject(new Error(data.error || '未知错误'))
      }
    }
    
    window.addEventListener('message', messageHandler)
    window.parent.postMessage({
      source: 'plugin-frontend',
      action: 'ipc-invoke',
      id,
      payload: { channel, args }
    }, '*')
  })
}
```

#### 5.2 前端调用示例

```javascript
// 前端代码示例

// 1. 调用插件后端事件
const result = await triggerEvent('taobao-login', { args: [] })

// 2. 调用主进程 IPC 方法
const filePath = await invokeIpc('select-file')

// 3. 选择文件夹
const folderPath = await invokeIpc('select-folder')

// 4. 下载图片为 ZIP
await invokeIpc('download-images-as-zip', [imageList])
```

#### 5.3 前端构建配置

```typescript
// frontend/vite.config.ts

import { defineConfig } from 'vite'
import { resolve } from 'path'

export default defineConfig({
  root: __dirname,
  base: '',  // 重要：必须为空字符串，确保相对路径
  build: {
    outDir: resolve(__dirname, '../dist'),  // 输出到插件根目录的 dist/
    emptyOutDir: true
  },
  server: {
    host: '127.0.0.1',
    port: 5175  // 开发服务器端口，需与 manifest.json 一致
  }
})
```

**关键配置说明**:
- `base: ''` - 确保打包后的资源使用相对路径
- `outDir: '../dist'` - 输出到插件根目录的 dist 文件夹
- `port` - 必须与 manifest.json 中的 devServerUrl 端口一致

---

## 通信机制

### 1. 通信层级

```
插件前端 (iframe)
    ↕ PostMessage
主应用渲染进程 (PluginView.vue)
    ↕ IPC (contextBridge)
主应用主进程 (main.ts + ipcHandlers.ts)
    ↕ child_process.fork() + process.send/on
插件子进程 (plugin_host.js + nodes)
```

### 2. PostMessage 通信协议

#### 2.1 插件前端 → 主应用

```typescript
// 触发插件事件
{
  source: 'plugin-frontend',
  action: 'trigger-event',
  id: 1,  // 自增ID，用于匹配响应
  payload: {
    eventType: 'taobao-login',
    params: { args: [] }
  }
}

// 调用 IPC 方法
{
  source: 'plugin-frontend',
  action: 'ipc-invoke',
  id: 2,
  payload: {
    channel: 'select-file',
    args: []
  }
}
```

#### 2.2 主应用 → 插件前端

```typescript
// 响应
{
  source: 'host',
  id: 1,  // 匹配请求 ID
  success: true,
  result: { /* 结果数据 */ }
}

// 错误响应
{
  source: 'host',
  id: 2,
  success: false,
  error: '错误信息'
}
```

### 3. IPC 通信协议

```typescript
// 渲染进程 → 主进程
ipcRenderer.invoke('trigger-event', pluginName, eventType, params)

// 主进程响应
return { success: true, result: { /* 数据 */ } }
```

### 4. 子进程通信协议

#### 4.1 主进程 → 插件子进程

```javascript
// 执行事件
{
  jsFile: 'nodes/login.js',
  call: {
    class: 'LoginNode',
    method: 'startLogin',
    args: ['param1', 'param2']
  }
}

// 停止信号
{
  type: 'stop'
}
```

#### 4.2 插件子进程 → 主进程

```javascript
// 成功响应
{
  success: true,
  result: { /* 执行结果 */ }
}

// 失败响应
{
  success: false,
  error: '错误信息'
}

// 停止完成通知
{
  type: 'stopped'
}
```

---

## 路径管理

### 1. 开发环境 vs 打包环境

```
开发环境:
  项目根目录/
  ├── electron/
  ├── src/
  ├── plugins/
  │   ├── plugin-a/
  │   └── plugin-b/
  ├── ffmpeg.exe
  └── chromium-1181/

打包环境 (Windows):
  安装目录/
  ├── AppName.exe
  └── resources/
      ├── app.asar
      ├── plugins/
      │   ├── plugin-a/
      │   └── plugin-b/
      ├── ffmpeg.exe
      └── chromium-1181/
```

### 2. PathManager 适配策略

```typescript
class PathManager {
  constructor() {
    // 只有在真正打包后才认为是打包环境
    this.isPackaged = !!(
      process.resourcesPath && 
      !process.resourcesPath.includes('node_modules')
    )
  }

  getPluginsDir(): string {
    if (this.isPackaged) {
      return path.join(process.resourcesPath, 'plugins')
    } else {
      return path.join(__dirname, '..', 'plugins')
    }
  }
}
```

### 3. 外部依赖路径

```typescript
// FFmpeg 路径查找顺序
1. 打包环境: resources/ffmpeg.exe
2. 开发环境: 项目根目录/ffmpeg.exe
3. 当前工作目录: cwd/ffmpeg.exe

// Chrome 路径查找顺序
1. 打包环境: resources/chromium-1181/chrome-win/chrome.exe
2. 开发环境: 项目根目录/chromium-1181/chrome-win/chrome.exe
3. 当前工作目录: cwd/chromium-1181/chrome-win/chrome.exe
```

---

## 开发工作流

### 1. 开发新插件

#### Step 1: 创建插件目录结构

```bash
mkdir plugins/my-plugin
cd plugins/my-plugin

# 创建基本结构
mkdir nodes utils frontend dist
touch manifest.json plugin_host.js
```

#### Step 2: 配置 manifest.json

```json
{
  "name": "我的插件",
  "version": "1.0.0",
  "description": "插件描述",
  "events": [
    {
      "id": "my-event",
      "class": "MyNode",
      "method": "myMethod",
      "jsFile": "nodes/my-node.js"
    }
  ],
  "frontend": {
    "devServerUrl": "http://127.0.0.1:5175",
    "entry": "dist/index.html"
  }
}
```

#### Step 3: 复制 plugin_host.js

```bash
# 从其他插件复制标准的 plugin_host.js
cp ../taobao-scraper-plugin/plugin_host.js .
```

#### Step 4: 开发后端节点

```javascript
// nodes/my-node.js

class MyNode {
  async myMethod(param1, param2) {
    try {
      console.log('执行业务逻辑', param1, param2)
      
      // 业务逻辑实现
      const result = await this.processData(param1, param2)
      
      return {
        success: true,
        data: result,
        message: '执行成功'
      }
    } catch (error) {
      console.error('执行失败:', error)
      return {
        success: false,
        message: error.message
      }
    }
  }

  async processData(param1, param2) {
    // 具体实现
    return { param1, param2 }
  }
}

exports.MyNode = MyNode
```

#### Step 5: 开发前端界面

```bash
cd frontend

# 初始化前端项目
npm init -y
npm install vite

# 创建文件
touch index.html main.js vite.config.ts
```

**vite.config.ts**:
```typescript
import { defineConfig } from 'vite'
import { resolve } from 'path'

export default defineConfig({
  root: __dirname,
  base: '',
  build: {
    outDir: resolve(__dirname, '../dist'),
    emptyOutDir: true
  },
  server: {
    host: '127.0.0.1',
    port: 5175
  }
})
```

**main.js**:
```javascript
// 复制通信封装代码
let seq = 0
const triggerEvent = async (eventType, params = {}) => { /* ... */ }
const invokeIpc = async (channel, args = []) => { /* ... */ }

// 业务逻辑
const App = {
  setup() {
    const onAction = async () => {
      const result = await triggerEvent('my-event', { 
        args: ['param1', 'param2'] 
      })
      console.log('结果:', result)
    }
    
    return { onAction }
  }
}

// 挂载应用
const app = createApp(App)
if (window.ElementPlus) app.use(window.ElementPlus)
app.mount('#app')
```

#### Step 6: 开发调试

```bash
# 终端1: 启动主应用
npm run dev:start

# 终端2: 启动插件前端开发服务器
cd plugins/my-plugin/frontend
npm run dev
```

#### Step 7: 构建插件

```bash
cd plugins/my-plugin/frontend
npm run build

# 构建产物输出到 plugins/my-plugin/dist/
```

### 2. 调试技巧

#### 2.1 主进程日志

```typescript
// electron/logger.ts
logger.log('信息日志')
logger.warn('警告日志')
logger.error('错误日志')

// 日志文件位置: app-debug.log
```

#### 2.2 插件子进程日志

```javascript
// 插件中的 console.log 会输出到主进程控制台
console.log('插件日志')
console.error('插件错误')
```

#### 2.3 前端调试

```javascript
// 插件前端的 console.log 在浏览器开发者工具中查看
console.log('前端日志')

// 在主应用中按 F12 打开开发者工具
// 切换到 iframe 上下文查看插件前端日志
```

#### 2.4 PostMessage 通信调试

```javascript
// 在插件前端添加消息监听
window.addEventListener('message', (event) => {
  console.log('[DEBUG] 收到消息:', event.data)
})

// 在 PluginView.vue 中添加日志
console.log('[DEBUG] 发送消息:', data)
```

---

## 最佳实践

### 1. 插件开发规范

#### 1.1 命名规范

```
插件文件夹: kebab-case (my-plugin)
类名: PascalCase (MyNode)
方法名: camelCase (myMethod)
事件ID: kebab-case (my-event)
```

#### 1.2 错误处理

```javascript
// ✅ 推荐：完善的错误处理
async myMethod() {
  try {
    const result = await riskyOperation()
    return { success: true, data: result }
  } catch (error) {
    console.error('操作失败:', error)
    return { 
      success: false, 
      message: error.message || '操作失败' 
    }
  }
}

// ❌ 不推荐：没有错误处理
async myMethod() {
  const result = await riskyOperation()
  return result
}
```

#### 1.3 资源管理

```javascript
// ✅ 推荐：及时释放资源
class MyNode {
  async processData() {
    const browser = await playwright.chromium.launch()
    try {
      const page = await browser.newPage()
      // 处理逻辑
      return { success: true }
    } finally {
      await browser.close()  // 确保关闭浏览器
    }
  }
}

// ❌ 不推荐：资源泄漏
class MyNode {
  async processData() {
    const browser = await playwright.chromium.launch()
    const page = await browser.newPage()
    // 处理逻辑
    return { success: true }
    // 浏览器没有关闭！
  }
}
```

#### 1.4 异步操作

```javascript
// ✅ 推荐：使用 async/await
async myMethod() {
  const result1 = await operation1()
  const result2 = await operation2(result1)
  return { success: true, data: result2 }
}

// ✅ 推荐：并发执行
async myMethod() {
  const [result1, result2] = await Promise.all([
    operation1(),
    operation2()
  ])
  return { success: true, data: { result1, result2 } }
}
```

### 2. 通信优化

#### 2.1 避免频繁通信

```javascript
// ❌ 不推荐：每个操作都通信
for (let i = 0; i < 100; i++) {
  await triggerEvent('process-item', { args: [items[i]] })
}

// ✅ 推荐：批量处理
await triggerEvent('process-items', { args: [items] })
```

#### 2.2 超时设置

```javascript
// 根据任务复杂度设置合理的超时时间
const triggerEvent = async (eventType, params, timeout = 600000) => {
  // 默认 10 分钟超时
  // 长时间任务可以增加超时时间
}
```

### 3. 性能优化

#### 3.1 子进程复用

插件管理器会自动复用子进程，避免频繁创建销毁：

```typescript
// ✅ 自动优化：首次调用创建，后续调用复用
await pluginManager.triggerEvent('my-plugin', 'event1', {})
await pluginManager.triggerEvent('my-plugin', 'event2', {})  // 复用子进程
```

#### 3.2 静态资源优化

```typescript
// vite.config.ts
export default defineConfig({
  build: {
    minify: 'terser',  // 压缩代码
    rollupOptions: {
      output: {
        manualChunks: {
          // 分离第三方库
          vendor: ['vue', 'element-plus']
        }
      }
    }
  }
})
```

### 4. 安全性

#### 4.1 IPC 白名单

```json
// config.json
{
  "ipcAllowlist": [
    "select-file",
    "select-folder"
    // 只暴露必要的 IPC 通道
  ]
}
```

#### 4.2 路径验证

```typescript
// PathManager 自动防止路径遍历攻击
const safePath = path.normalize(path.join(pluginsRoot, pathname))
if (!safePath.startsWith(pluginsRoot)) {
  throw new Error('Forbidden')
}
```

### 5. 测试建议

#### 5.1 单元测试

```javascript
// nodes/__tests__/my-node.test.js
const { MyNode } = require('../my-node')

describe('MyNode', () => {
  it('should process data correctly', async () => {
    const node = new MyNode()
    const result = await node.myMethod('test', 123)
    expect(result.success).toBe(true)
  })
})
```

#### 5.2 集成测试

```javascript
// 测试插件与主应用的集成
describe('Plugin Integration', () => {
  it('should trigger event successfully', async () => {
    const result = await pluginManager.triggerEvent(
      'my-plugin', 
      'my-event', 
      { args: ['test'] }
    )
    expect(result.success).toBe(true)
  })
})
```

---

## 附录

### A. 常见问题

#### Q1: 插件无法加载？

**检查清单**:
1. `manifest.json` 格式是否正确
2. `plugin_host.js` 文件是否存在
3. 插件文件夹名称是否正确
4. 查看控制台错误日志

#### Q2: 前端通信失败？

**检查清单**:
1. `triggerEvent` 和 `invokeIpc` 函数是否正确实现
2. 事件 ID 是否与 `manifest.json` 一致
3. 查看浏览器控制台的 PostMessage 日志
4. 确认 IPC 通道是否在白名单中

#### Q3: 子进程调用超时？

**可能原因**:
1. 后端方法执行时间过长（增加超时时间）
2. 方法抛出未捕获的异常（检查错误处理）
3. 异步操作没有 await（检查异步逻辑）

#### Q4: 打包后插件路径错误？

**解决方案**:
1. 确保使用 `pathManager` 获取路径
2. 检查 `electron-builder` 配置中的 `extraResources`
3. 验证打包后的目录结构

### B. 项目依赖

#### 主应用依赖

```json
{
  "dependencies": {
    "element-plus": "^2.4.0",
    "pinia": "^2.1.0",
    "vue": "^3.3.0",
    "vue-router": "^4.2.0",
    "archiver": "^6.0.1",
    "fluent-ffmpeg": "^2.1.3",
    "sharp": "^0.34.3",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "electron": "^37.2.3",
    "electron-builder": "^26.0.12",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
```

#### 插件常用依赖

```json
{
  "dependencies": {
    "playwright": "^1.40.0",
    "xlsx": "^0.18.5",
    "sharp": "^0.34.3"
  }
}
```

### C. 相关资源

- [Electron 文档](https://www.electronjs.org/docs/latest/)
- [Vue 3 文档](https://cn.vuejs.org/)
- [Vite 文档](https://cn.vitejs.dev/)
- [Playwright 文档](https://playwright.dev/)
- [Element Plus 文档](https://element-plus.org/zh-CN/)

---

**文档版本**: v1.0.0  
**最后更新**: 2026-02-10  
**维护者**: 优矩互动

